#include "main.h"

#include <stdio.h>
/**
 * _isupper - checks if a  character is uppercase or not
 * Return: Always 0.
 */
int _isupper(int c)
{
	if (c <= 67)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
